---
task: prd/claude-md-hierarchy/tasks/04-do-task-session-handoff.md
date: 2026-07-04
outcome: done-with-deviations
tags: [tool-gap, manual-correction, clean]
tools-used: [Bash, Read, Edit, AskUserQuestion, Skill, Write, Agent]
tools-requested-but-missing: []
modules-touched: []
---

## What happened

Kicked off by the human message "please implement 04-do-task-session-handoff". The assistant first read the task file, `plan.md`, `.claude/skills` layout, recent git log, and both `do-task/SKILL.md` and `debrief-task/SKILL.md` for context, then confirmed `$CLAUDE_CODE_SESSION_ID` is a real available env var (`grep -rn "CLAUDE_CODE_SESSION_ID\|SESSION_ID" .claude; env | grep -i claude`, `toolu_01WtynzELfwL6XechtG44ksK`) before writing anything.

The core deliverable is one `Edit` to `.claude/skills/do-task/SKILL.md`'s step 5 (`toolu_016JDvAsxA5sza1k9miLqwKu`): "resolve and report" now reads, after the existing done/in-progress branching, "If and only if you set `status: done`: also record `session: $CLAUDE_CODE_SESSION_ID` ... into the task file's frontmatter, then spawn a background Agent invoking `/debrief-task <prd-slug> <task>` for the task just completed. Don't wait on it — it runs independently in a fresh context." This is the entire functional change; the rest of the session is validating it.

Before marking task 04 done, its own acceptance criteria required a live end-to-end `do-task` run to prove the mechanism works. Rather than pick a task file unilaterally, the assistant used `AskUserQuestion` (`toolu_014SWfymXDjJsMsQPmQBNYTS`) — "Task 04's acceptance criteria requires running do-task end-to-end on a real small task... Task 05... is unblocked, small, and legitimate work in this same PRD" — offering to run it, have the human run it, or skip live validation. The human picked "Run /do-task on task 05 (Recommended)."

The assistant then tried to self-invoke: `Skill({skill:"do-task", args:"claude-md-hierarchy 05"})` (`toolu_01XUBJNqVSSuFSBCGEGSekMk`), which failed outright: `"<tool_use_error>Skill do-task cannot be used with Skill tool due to disable-model-invocation</tool_use_error>"`. Both `do-task/SKILL.md` and `debrief-task/SKILL.md` carry `disable-model-invocation: true` (visible from the earlier Read of each), which blocks exactly this kind of self-invocation — only the real slash command works. The human's next turn was the actual `<command-name>/do-task</command-name>` slash-command invocation with args `05-review-debrief-scoped-recommendations`, i.e. the human ran the real command themselves rather than the assistant working around the block.

From there `do-task`'s own process took over and ran task 05 (updating `review-debrief/SKILL.md`'s scoping logic, with two rounds of dry-run-driven fixes) to completion, stamping task 05's frontmatter with `status: done` + `session: 896d99f8-9f98-42f6-b179-2e10c3fd9b10` and spawning a background debrief Agent for it (`toolu_01DZGHbjCnFGEDJsbkhnJuoY`). That inner arc is covered in full in `prd/claude-md-hierarchy/debriefs/05-review-debrief-scoped-recommendations.md` and is not re-derived here.

Returning to task 04: the assistant re-read the task file, checked off the first three acceptance criteria, and set the fourth to a partial note pending confirmation ("Awaiting confirmation the debrief file was actually written before checking this off", `toolu_01UzmBJo18uJnNvzpXmpV87C`), set `status: in-progress` (`toolu_013SeGVnUBDhp48vy9HbjTqQ`), then polled (`ls .../debriefs/; git status --short`, `toolu_0186ANLi2eKTWqNkSGbRjLK3`) until the task-05 debrief agent's completion notification arrived (a `task-notification` embedded as a user-turn message reporting the agent wrote `prd/claude-md-hierarchy/debriefs/05-review-debrief-scoped-recommendations.md`). Only then did it check off the fourth criterion with the concrete evidence (`toolu_01KGd1LxVaz9dQrBrWKCs136`), set `status: done` + `session: 896d99f8-9f98-42f6-b179-2e10c3fd9b10` on task 04's own frontmatter (`toolu_011h5eKoiVn988iysabcanSD`), and — applying the very mechanism it had just built — spawned a second background Agent, "Debrief task 04 session" (`toolu_01MRzu7iyGMVcaKNQoWvwzcC`), to debrief itself. That spawned agent is this debrief's own invocation.

## Deviations from plan

- **Validation vehicle chosen live, not pre-specified.** The task file names no specific task for the required end-to-end run; the assistant proposed task 05 via `AskUserQuestion` and the human confirmed it. Not a scope change to task 04 itself, but a decision point the task file left open.
- **Invocation mechanism forced a detour.** The task file's plan assumes `do-task` can straightforwardly be run for validation; it doesn't anticipate that the assistant validating its own new mechanism would be unable to invoke `do-task` itself. The `Skill` tool attempt errored on `disable-model-invocation`, so the human had to run the real `/do-task` slash command in their own turn instead of the assistant driving the validation end-to-end unattended.
- **Acceptance-criterion checkoff was staged, not immediate**, reflecting genuine uncertainty rather than a plan change: the fourth criterion was left partially-checked with an explicit "awaiting confirmation" note until the spawned task-05 debrief agent's completion was actually observed, per the assistant's own text: "Task 04 is left `in-progress` with 3/4 criteria checked; I'm holding off marking it `done` until the background debrief agent confirms it actually wrote `prd/claude-md-hierarchy/debriefs/05-review-debrief-scoped-recommendations.md`."

## Friction points

- **tool-gap** — `Skill({skill:"do-task", args:"claude-md-hierarchy 05"})` errored: `"Skill do-task cannot be used with Skill tool due to disable-model-invocation"` (`toolu_01XUBJNqVSSuFSBCGEGSekMk`). Both `do-task` and `debrief-task` carry `disable-model-invocation: true`, which is exactly what makes them safe to spawn as background agents from elsewhere but also means an agent can't self-invoke them via the `Skill` tool — only a real slash-command turn works. The assistant's own closing summary flags this as a structural, recurring constraint: "Deviation worth flagging: `do-task` and `debrief-task` are both `disable-model-invocation: true`, so I couldn't self-invoke `/do-task` via the Skill tool — I had to ask you and you ran the actual `/do-task claude-md-hierarchy 05` command yourself... worth keeping in mind for task 06's rollout." This error doesn't match the `tools-requested-but-missing` scan pattern (no "requires approval" / permission-grant phrasing), so it isn't listed in that frontmatter field despite being a real gap — same finding already surfaced in task 05's own debrief.
- **manual-correction** — immediately after the `Skill` error, the next user-turn is the real `<command-name>/do-task</command-name>` slash-command invocation with args `05-review-debrief-scoped-recommendations` — a human action that substituted for the blocked self-invocation, not free-text correction, but the thing that actually unblocked the session. (This is the same slash-command artifact filtered out of "human string message" evidence per the skill's noise rule; it's cited here as the resolving action for the tool-gap above, not double-counted as a separate spoken correction.)
- **clean** — task 04's own functional change (the `do-task/SKILL.md` step-5 edit) went in as a single, unambiguous `Edit` with no retries, errors, or rework; all friction in this session traces to validating it, not to writing it.

Modules touched: none — every `Edit`/`Write` in this session targeted `.claude/skills/do-task/SKILL.md`, `.claude/skills/review-debrief/SKILL.md`, `prd/claude-md-hierarchy/tasks/*.md`, and scratchpad test fixtures, none under `src/`.
